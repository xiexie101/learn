# 新闻爬虫优化设计方案

## 背景
优化 `news_crawler.py`，增加快捷获取当天新闻的功能，并按日期归档保存。

## 改动点

### 1. 交互式日期输入
- 运行后提示：`直接回车获取当天新闻，或输入日期 (YYYY-MM-DD)：`
- 回车默认使用当天日期

### 2. 按日期保存到独立文件夹
- 路径：`pythonCode/news/YYYY-MM-DD.md`
- 自动创建 `news/` 目录

### 3. 代码优化
- 移除未使用的 import
- 使用 `asyncio.run()` 替代废弃 API
- 更友好的输出提示
